apiVersion: batch/v1
kind: Job
metadata:
  name: jobintel-liveproof
  namespace: jobintel
  labels:
    app.kubernetes.io/name: jobintel
    app.kubernetes.io/component: job
    jobintel.io/purpose: liveproof
spec:
  backoffLimit: 0
  activeDeadlineSeconds: 3600
  template:
    metadata:
      labels:
        app.kubernetes.io/name: jobintel
        app.kubernetes.io/component: job
        jobintel.io/purpose: liveproof
    spec:
      serviceAccountName: jobintel
      restartPolicy: Never
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
      containers:
        - name: jobintel
D8622080012.dkr.ecr.us-east-1.amazonaws.com/jobintel:irsa-fix-20260204-212335
          imagePullPolicy: IfNotPresent
          workingDir: /app
          command:
            - python
            - scripts/run_daily.py
          args:
            - --profiles
            - cs
            - --us_only
            - --no_post
          env:
            - name: CAREERS_MODE
              value: "LIVE"
            - name: PUBLISH_S3_REQUIRE
              value: "1"
          envFrom:
            - configMapRef:
                name: jobintel-config
            - secretRef:
                name: jobintel-secrets
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "2Gi"
          securityContext:
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: work
              mountPath: /work
            # Mount /app/data for writable pipeline outputs.
            - name: jobintel-data
              mountPath: /app/data
            - name: jobintel-state
              mountPath: /app/state
      volumes:
        - name: tmp
          emptyDir: {}
        - name: work
          emptyDir: {}
        - name: jobintel-data
          emptyDir: {}
        - name: jobintel-state
          emptyDir: {}
