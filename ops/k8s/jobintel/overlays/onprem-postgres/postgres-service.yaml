apiVersion: v1
kind: Service
metadata:
  name: jobintel-postgres
  namespace: jobintel
  labels:
    app.kubernetes.io/name: jobintel-postgres
spec:
  selector:
    app.kubernetes.io/name: jobintel-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
