version: "3.9"

services:
  jobintel:
    build: .
    image: jobintel:local
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    command: ["scripts/run_daily.py", "--profiles", "cs,tam,se", "--us_only", "--no_post"]
    working_dir: /app

